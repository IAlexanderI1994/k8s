apiVersion: v1
kind: Pod
metadata:
  name: client-pod
  labels:
    component: web
spec:
  containers:
    - name: client
      image: alexkir1994/worker-prod
      ports:
        - containerPort: 3000
